# .markdownlintignore — RedRoomRewards
# Updated: 2026-04-19 (config refresh — legacy copy in LEGACY_CONFIGS/)
# TODO: Remove entries that no longer apply as the project structure changes.

# Exclude application code directories from markdown linting
src/
api/
infra/

# Exclude build artifacts and dependencies
node_modules/
dist/
build/
out/
.next/
coverage/
.venv/
vendor/

# Exclude archived configs (may contain non-standard markdown)
LEGACY_CONFIGS/

# Exclude archived / historical documents — lint rules in force when they were
# authored may not match current config; archives are preserved as-is and not
# live-edited. Matches the LEGACY_CONFIGS/ pattern above.
docs/history/
archive/

# Exclude Super-Linter output
super-linter-output/
